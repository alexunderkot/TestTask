{
    "name": "C++ Echo Server",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-vscode.cpptools",
                "ms-vscode.cmake-tools"
            ]
        }
    },
    "forwardPorts": [8080],
    "postCreateCommand": "mkdir -p build && cd build && cmake .. && make"
}