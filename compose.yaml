version: '3.8'

services:
  server:
    build:
      context: .
      dockerfile: .devcontainer/Dockerfile
    container_name: calc-server
    ports:
      - "8080:8080"
    command: calc-server 8080
    networks:
      - calc-network

networks:
  calc-network:
    driver: bridge